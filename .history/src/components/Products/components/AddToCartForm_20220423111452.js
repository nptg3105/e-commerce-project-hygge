import PropTypes from 'prop-types'
import React from 'react'

const AddToCartForm = ({ onSubmit = null }) => <div />

AddToCartForm.propTypes = {
	onSubmit: PropTypes.func.isRequired
}

export default AddToCartForm
